{
  "name": "@app/contract",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared contract — error codes and the permission catalog, usable from both Node and the browser",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./rbac": "./src/rbac.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^4.1.0"
  }
}
