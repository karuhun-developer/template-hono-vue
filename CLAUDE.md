# Hono + Vue Template

Instructions for agents working in this repository live in [`AGENTS.md`](AGENTS.md). Read it before writing code.

It is a real file rather than a symlink so that it survives `core.symlinks=false` on Windows, and a pointer rather than a copy so that the two can never disagree.
